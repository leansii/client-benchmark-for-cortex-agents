# Cortex Agent Benchmark placeholder configuration.
# Copy to .env.local (gitignored) for local development.

# Proxy configuration
PROXY_PORT=3000
LOG_LEVEL=debug
RATE_LIMIT_MAX_RPS=5
RATE_LIMIT_BURST=10

# Snowflake credentials (placeholders only - do not commit real secrets)
SNOWFLAKE_ACCOUNT=acct_placeholder
SNOWFLAKE_USER=dev_user
SNOWFLAKE_ROLE=DEV_ROLE
SNOWFLAKE_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----FAKE-----END PRIVATE KEY-----

# JWT configuration
JWT_SIGNING_KEY=dev-signing-key-placeholder
JWT_AUDIENCE=dev-clients
JWT_ISSUER=dev-proxy

# Telemetry / Bench
BENCH_BASELINE_URL=http://localhost:3000
TELEMETRY_EXPORT_URL=http://localhost:4318
